<%= form_with(model: order_alignment, local: true) do |form| %>
  <% if order_alignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_alignment.errors.count, "error") %> prohibited this order_alignment from being saved:</h2>

      <ul>
      <% order_alignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :order_alignment_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

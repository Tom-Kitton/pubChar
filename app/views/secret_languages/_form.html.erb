<%= form_with(model: secret_language, local: true) do |form| %>
  <% if secret_language.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(secret_language.errors.count, "error") %> prohibited this secret_language from being saved:</h2>

      <ul>
      <% secret_language.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :secret_language_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

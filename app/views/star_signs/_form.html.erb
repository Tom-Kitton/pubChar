<%= form_with(model: star_sign, local: true) do |form| %>
  <% if star_sign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(star_sign.errors.count, "error") %> prohibited this star_sign from being saved:</h2>

      <ul>
      <% star_sign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :star_sign_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<p id="notice"><%= notice %></p>

<h1>Moral Alignments</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @moral_alignments.each do |moral_alignment| %>
      <tr>
        <td><%= moral_alignment.content %></td>
        <td><%= link_to 'Show', moral_alignment %></td>
        <td><%= link_to 'Edit', edit_moral_alignment_path(moral_alignment) %></td>
        <td><%= link_to 'Destroy', moral_alignment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Moral Alignment', new_moral_alignment_path %>

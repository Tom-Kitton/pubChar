<%= form_with(model: ethnicity, local: true) do |form| %>
  <% if ethnicity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ethnicity.errors.count, "error") %> prohibited this ethnicity from being saved:</h2>

      <ul>
      <% ethnicity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :ethnicity_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

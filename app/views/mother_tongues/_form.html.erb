<%= form_with(model: mother_tongue, local: true) do |form| %>
  <% if mother_tongue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mother_tongue.errors.count, "error") %> prohibited this mother_tongue from being saved:</h2>

      <ul>
      <% mother_tongue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :mother_tongue_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

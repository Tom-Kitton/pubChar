<%= form_with(model: skin_base_tone, local: true) do |form| %>
  <% if skin_base_tone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skin_base_tone.errors.count, "error") %> prohibited this skin_base_tone from being saved:</h2>

      <ul>
      <% skin_base_tone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :skin_base_tone_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
